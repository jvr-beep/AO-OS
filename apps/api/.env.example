# AO OS API environment template
# Copy to .env for local development or to your secret manager for production.

# Runtime
NODE_ENV=production
PORT=4000

# Database
DATABASE_URL=postgresql://USER:PASSWORD@HOST:5432/DB_NAME?sslmode=require

# Auth
AUTH_JWT_SECRET=replace-with-strong-random-secret
AUTH_JWT_EXPIRES_IN=1h

# App URLs
APP_BASE_URL=https://app.aosanctuary.com

# Google OAuth
GOOGLE_CLIENT_ID=replace-me
GOOGLE_CLIENT_SECRET=replace-me
GOOGLE_REDIRECT_URI=https://api.aosanctuary.com/v1/auth/google/callback

# Email (optional but recommended)
EMAIL_FROM=AO OS <noreply@aosanctuary.com>
RESEND_API_KEY=replace-me

# Optional admin bootstrap
AUTH_SEED_ADMIN_EMAIL=
AUTH_SEED_ADMIN_PASSWORD=
AUTH_SEED_ADMIN_NAME=Seed Admin

# Datadog (optional — unhandled errors are forwarded to Datadog when set)
AO_DATA_DOG_KEY=replace-me

# Ops monitor key — must match AO_OS_MONITOR_KEY GitHub Actions secret
# Used by MonitorKeyGuard to authenticate the self-heal.yml workflow
MONITOR_API_KEY=replace-with-strong-random-secret